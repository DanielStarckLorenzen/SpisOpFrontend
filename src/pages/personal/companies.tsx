import {User} from "../../types/User.ts";

export type CompaniesProps = {
  user: User;
}

const Companies = () => {

  return (
    <div>
      <h1>Companies</h1>
    </div>
  );
}

export default Companies;
